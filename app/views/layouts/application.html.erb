<!DOCTYPE html>
<html>
  <head>
    <title>CepTracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="p-0 m-0 max-h-fit bg-gray-50 text-gray-900 font-sans antialiased">
    <nav class="bg-gray-800 p-4 w-screen">
      <div class="container mx-auto flex justify-between items-center">
        <a href="<%= root_path %>" class="text-white font-bold text-lg">CepTracker</a>
        <div>
          <a href="<%= root_path %>" class="text-white mr-4">Home</a>
          <a href="<%= statistics_path %>" class="text-white">Statistics</a>
        </div>
      </div>
    </nav>
    <main class="w-screen">
      <%= yield %>
    </main>
  </body>
</html>
