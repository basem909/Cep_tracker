<!DOCTYPE html>
<html>
<head>
  <title>Statistics</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>
<body class="bg-gray-50 text-gray-900 font-sans antialiased">
  <div class="container mx-auto p-8">
    <h1 class="text-4xl font-extrabold mb-8 text-gray-800">Statistics</h1>
    
    <div class="bg-white p-8 rounded-lg shadow-md mb-8">
      <h2 class="text-2xl font-semibold mb-4 text-gray-700">Most Searched CEPs</h2>
      <ul class="list-disc pl-5 text-gray-600">
        <% @most_searched_ceps.each do |cep, count| %>
          <% unless cep.blank? %>
            <li><span class="font-medium text-gray-800"><%= cep %></span>: <%= count %> searches</li>
          <% end %>
        <% end %>
      </ul>
    </div>

    <div class="bg-white p-8 rounded-lg shadow-md">
      <h2 class="text-2xl font-semibold mb-4 text-gray-700">CEP Searches by State</h2>
      <ul class="list-disc pl-5 text-gray-600">
        <% @ceps_by_state.each do |state, count| %>
          <% unless state.blank? %>
            <li><span class="font-medium text-gray-800"><%= state %></span>: <%= count %> searches</li>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
</body>
</html>
